<UserControl x:Class="SphericalImageViewer.UI.MainDockPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:SphericalImageViewer.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1400">

    <UserControl.Resources>
        <!-- Converters -->
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <converters:BoolToColorConverter x:Key="BoolToColorConverter"/>
        <converters:BoolToStatusConverter x:Key="BoolToStatusConverter"/>

        <!-- Modern Dark Theme Colors -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#FF0078D4"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#FF106EBE"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF00BCF2"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="White"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
        <SolidColorBrush x:Key="OnSurfaceBrush" Color="#212121"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>

        <!-- Modern Card Style -->
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="2" Opacity="0.1" Color="#000000"/>
                </Setter.Value>
            </Setter>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="16"/>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                x:Name="Border">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Icon Button Style -->
        <Style x:Key="IconButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource OnSurfaceBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="44"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                x:Name="Border">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F0F0F0"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                x:Name="Border">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Loading Animation -->
        <Storyboard x:Key="LoadingAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="LoadingRing"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                           From="0" To="360" Duration="0:0:1"/>
        </Storyboard>

    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🌐" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock Text="Spherical Image Viewer" 
                               Foreground="White" 
                               FontSize="20" 
                               FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Settings" 
                            Command="{Binding OpenSettingsCommand}"
                            Style="{StaticResource ModernButton}"
                            Background="Transparent"
                            BorderBrush="White"
                            BorderThickness="1"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Left Panel - Image Viewer -->
            <Border Grid.Column="0" Style="{StaticResource ModernCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Connection Panel -->
                    <GroupBox Grid.Row="0" Header="Server Connection">
                        <Grid Margin="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Server:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Medium"/>
                            <TextBox Grid.Column="1" Text="{Binding ServerUrl}" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0"/>
                            <Button Grid.Column="2" Content="Connect" 
                                    Command="{Binding ConnectCommand}"
                                    Style="{StaticResource ModernButton}"
                                    Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}, ConverterParameter=Inverse}"/>
                            <Button Grid.Column="3" Content="Disconnect" 
                                    Command="{Binding DisconnectCommand}"
                                    Style="{StaticResource ModernButton}"
                                    Background="#DC3545"
                                    Visibility="{Binding IsConnected, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                        </Grid>
                    </GroupBox>

                    <!-- Image Directory and Load -->
                    <GroupBox Grid.Row="1" Header="Image Source" Margin="0,8,0,0">
                        <Grid Margin="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Directory:" VerticalAlignment="Center" Margin="0,0,8,0" FontWeight="Medium"/>
                            <TextBox Grid.Column="1" Text="{Binding ImageDirectory}" Style="{StaticResource ModernTextBox}" Margin="0,0,8,0"/>
                            <Button Grid.Column="2" Content="Load Images" 
                                    Command="{Binding LoadImagesCommand}"
                                    Style="{StaticResource ModernButton}"
                                    IsEnabled="{Binding IsConnected}"/>
                        </Grid>
                    </GroupBox>

                    <!-- Image Display -->
                    <Border Grid.Row="2" 
                            BorderBrush="{StaticResource BorderBrush}" 
                            BorderThickness="2" 
                            Background="Black" 
                            CornerRadius="8"
                            Margin="0,16,0,8">
                        <Grid>
                            <!-- Image -->
                            <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                          VerticalScrollBarVisibility="Auto">
                                <Image Name="MainImage" 
                                       Source="{Binding CurrentImage}" 
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </ScrollViewer>

                            <!-- Loading Indicator -->
                            <Grid Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                                  Background="#80000000">
                                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Ellipse x:Name="LoadingRing" 
                                             Width="50" Height="50"
                                             Stroke="{StaticResource AccentBrush}"
                                             StrokeThickness="4"
                                             StrokeDashArray="8,4"
                                             RenderTransformOrigin="0.5,0.5">
                                        <Ellipse.RenderTransform>
                                            <RotateTransform/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <TextBlock Text="Loading..." Foreground="White" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Grid>

                            <!-- No Image Message -->
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding CurrentImage}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBlock Text="📷" FontSize="48" HorizontalAlignment="Center" Foreground="#CCCCCC"/>
                                <TextBlock Text="No image loaded" 
                                           FontSize="16" 
                                           Foreground="#999999" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,8,0,0"/>
                                <TextBlock Text="Connect to server and load images to begin" 
                                           FontSize="12" 
                                           Foreground="#777777" 
                                           HorizontalAlignment="Center" 
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Frame Navigation -->
                    <Border Grid.Row="3" 
                            Background="{StaticResource SurfaceBrush}"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="8"
                            Padding="16,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <!-- Navigation Buttons -->
                            <StackPanel Grid.Column="0" Orientation="Horizontal">
                                <Button Content="⏮️" 
                                        Command="{Binding FirstFrameCommand}"
                                        Style="{StaticResource IconButton}"
                                        ToolTip="First Frame"/>
                                <Button Content="◀️" 
                                        Command="{Binding PreviousFrameCommand}"
                                        Style="{StaticResource IconButton}"
                                        ToolTip="Previous Frame"/>
                                <Button Content="▶️" 
                                        Command="{Binding NextFrameCommand}"
                                        Style="{StaticResource IconButton}"
                                        ToolTip="Next Frame"/>
                                <Button Content="⏭️" 
                                        Command="{Binding LastFrameCommand}"
                                        Style="{StaticResource IconButton}"
                                        ToolTip="Last Frame"/>
                            </StackPanel>

                            <!-- Frame Info -->
                            <TextBlock Grid.Column="1" 
                                       Text="{Binding FrameInfo}" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       FontWeight="Medium"/>

                            <!-- Export Button -->
                            <Button Grid.Column="2" 
                                    Content="Export Results" 
                                    Command="{Binding ExportResultsCommand}"
                                    Style="{StaticResource ModernButton}"
                                    Background="#28A745">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource ModernButton}">
                                        <Setter Property="Background" Value="#28A745"/>
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DetectionResults.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </DataTrigger>
                                            <Trigger Property="DataContext" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" 
                          Width="4" 
                          Background="{StaticResource BorderBrush}"
                          HorizontalAlignment="Center"/>

            <!-- Right Panel - Controls -->
            <Border Grid.Column="2" Style="{StaticResource ModernCard}">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>

                        <!-- Camera Controls -->
                        <GroupBox Header="Camera Navigation" Margin="0,0,0,16">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Yaw -->
                                <Grid Grid.Row="0" Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Yaw:" VerticalAlignment="Center" FontWeight="Medium"/>
                                    <Slider Grid.Column="1" 
                                            Minimum="-180" Maximum="180" 
                                            Value="{Binding Yaw}" 
                                            TickFrequency="30" 
                                            IsSnapToTickEnabled="True"
                                            Margin="8,0"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Yaw, StringFormat=F1°}" 
                                               VerticalAlignment="Center" 
                                               MinWidth="40"
                                               FontFamily="Consolas"/>
                                </Grid>

                                <!-- Pitch -->
                                <Grid Grid.Row="1" Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Pitch:" VerticalAlignment="Center" FontWeight="Medium"/>
                                    <Slider Grid.Column="1" 
                                            Minimum="-90" Maximum="90" 
                                            Value="{Binding Pitch}" 
                                            TickFrequency="15" 
                                            IsSnapToTickEnabled="True"
                                            Margin="8,0"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Pitch, StringFormat=F1°}" 
                                               VerticalAlignment="Center" 
                                               MinWidth="40"
                                               FontFamily="Consolas"/>
                                </Grid>

                                <!-- Roll -->
                                <Grid Grid.Row="2" Margin="0,0,0,12">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Roll:" VerticalAlignment="Center" FontWeight="Medium"/>
                                    <Slider Grid.Column="1" 
                                            Minimum="-180" Maximum="180" 
                                            Value="{Binding Roll}" 
                                            TickFrequency="30" 
                                            IsSnapToTickEnabled="True"
                                            Margin="8,0"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Roll, StringFormat=F1°}" 
                                               VerticalAlignment="Center" 
                                               MinWidth="40"
                                               FontFamily="Consolas"/>
                                </Grid>

                                <!-- Field of View -->
                                <Grid Grid.Row="3" Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="FOV:" VerticalAlignment="Center" FontWeight="Medium"/>
                                    <Slider Grid.Column="1" 
                                            Minimum="30" Maximum="180" 
                                            Value="{Binding Fov}" 
                                            TickFrequency="15" 
                                            IsSnapToTickEnabled="True"
                                            Margin="8,0"/>
                                    <TextBlock Grid.Column="2" 
                                               Text="{Binding Fov, StringFormat=F1°}" 
                                               VerticalAlignment="Center" 
                                               MinWidth="40"
                                               FontFamily="Consolas"/>
                                </Grid>

                                <!-- Reset Button -->
                                <Button Grid.Row="4" 
                                        Content="Reset View" 
                                        Command="{Binding ResetViewCommand}"
                                        Style="{StaticResource ModernButton}"
                                        Background="#6C757D"/>

                                <!-- Quick Navigation Buttons -->
                                <UniformGrid Grid.Row="5" Columns="2" Margin="0,12,0,0">
                                    <Button Content="Up" Margin="2" Style="{StaticResource ModernButton}" Background="#17A2B8"/>
                                    <Button Content="Down" Margin="2" Style="{StaticResource ModernButton}" Background="#17A2B8"/>
                                    <Button Content="Left" Margin="2" Style="{StaticResource ModernButton}" Background="#17A2B8"/>
                                    <Button Content="Right" Margin="2" Style="{StaticResource ModernButton}" Background="#17A2B8"/>
                                </UniformGrid>
                            </Grid>
                        </GroupBox>

                        <!-- AI Detection Controls -->
                        <GroupBox Header="AI Detection" Margin="0,0,0,16">
                            <StackPanel Margin="8">
                                <!-- Model Selection -->
                                <TextBlock Text="Model:" FontWeight="Medium" Margin="0,0,0,4"/>
                                <ComboBox ItemsSource="{Binding AvailableModels}" 
                                          SelectedItem="{Binding SelectedModel}" 
                                          Margin="0,0,0,12"
                                          Padding="12,8"
                                          BorderThickness="2"
                                          BorderBrush="{StaticResource BorderBrush}"/>

                                <!-- Detection Text -->
                                <TextBlock Text="Detection Target:" FontWeight="Medium" Margin="0,0,0,4"/>
                                <TextBox Text="{Binding DetectionText}" 
                                         Style="{StaticResource ModernTextBox}" 
                                         Margin="0,0,0,12"/>

                                <!-- Action Buttons -->
                                <Button Content="Run Detection" 
                                        Command="{Binding RunDetectionCommand}"
                                        Style="{StaticResource ModernButton}"
                                        Margin="0,0,0,8"
                                        IsEnabled="{Binding IsConnected}"/>

                                <Button Content="Run Segmentation" 
                                        Command="{Binding RunSegmentationCommand}"
                                        Style="{StaticResource ModernButton}"
                                        Background="#FFC107"
                                        IsEnabled="{Binding IsConnected}"/>
                            </StackPanel>
                        </GroupBox>

                        <!-- Detection Results -->
                        <GroupBox Header="Detection Results">
                            <Grid Margin="8">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- Results Count -->
                                <TextBlock Grid.Row="0" 
                                           Text="{Binding DetectionResults.Count, StringFormat=Found {0} objects}"
                                           FontWeight="Medium"
                                           Margin="0,0,0,8"/>

                                <!-- Results List -->
                                <ListBox Grid.Row="1" 
                                         ItemsSource="{Binding DetectionResults}"
                                         MaxHeight="200"
                                         BorderThickness="1"
                                         BorderBrush="{StaticResource BorderBrush}">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <Border Background="#F8F9FA" 
                                                    CornerRadius="4" 
                                                    Padding="8" 
                                                    Margin="2">
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="0" 
                                                               Text="{Binding ObjectName}" 
                                                               FontWeight="Medium"/>
                                                    <TextBlock Grid.Column="1" 
                                                               Text="{Binding Confidence, StringFormat=P1}" 
                                                               Foreground="{StaticResource PrimaryBrush}"
                                                               FontFamily="Consolas"/>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Grid>
                        </GroupBox>

                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" 
                Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" 
                           Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Ellipse Width="12" Height="12" 
                             Fill="{Binding IsConnected, Converter={StaticResource BoolToColorConverter}}"
                             Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding IsConnected, Converter={StaticResource BoolToStatusConverter}}" 
                               VerticalAlignment="Center"
                               FontWeight="Medium"/>
                </StackPanel>
            </Grid>
        </Border>

    </Grid>
</UserControl>